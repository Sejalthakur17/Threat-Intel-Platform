# Copy this to terraform.tfvars and fill in your values
# NEVER commit terraform.tfvars to GitHub

aws_region      = "ap-south-1"
my_ip           = "YOUR_HOME_IP/32"    # e.g. "103.xx.xx.xx/32"
public_key_path = "~/.ssh/threat-intel.pub"
